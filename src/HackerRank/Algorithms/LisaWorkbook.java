package HackerRank.Algorithms;

/**
 * https://www.hackerrank.com/challenges/bear-and-workbook
 */
public class LisaWorkbook {
    /*
       Scanner in = new Scanner(System.in);
        int chapters=in.nextInt();
        int perPageQuestion=in.nextInt();
        int []questions=new int[chapters];
        for(int i=0;i<chapters;i++)
        {
            questions[i]=in.nextInt();
        }
        test(chapters,perPageQuestion,questions);



    }

    private static void test(int chapters,int perPageQuestion,int []questions) {
        int count=0;
        int pages=1;
        int j=1;
        for(int i=0;i<chapters;i++)
        {

            for(j=1;j<=questions[i];j++)
            {
                if(pages==j)
                    count++;
                if(j%perPageQuestion==0)
                    pages++;

            }
            if((j-1)%perPageQuestion!=0)
                pages++;


        }
        System.out.println(count);

    }


     */
}
